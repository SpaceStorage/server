# Negative fixture for non-goal audit

This document deliberately omits any mention of the refused SQL isolation
level name so the audit can prove `nongoal_unspecified` when a cited path
is silent.

Isolation discussion here only covers READ COMMITTED style wording without
naming the third, refused level.
