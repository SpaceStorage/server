# invalid: sync none is not durable (startup error)
node { name db-1; }
storage {
  data_dir "./data";
  sync none;
}
entrypoint { address 127.0.0.1; port 7601; handler admin; plaintext; }
disable postgresql;
disable redis;
