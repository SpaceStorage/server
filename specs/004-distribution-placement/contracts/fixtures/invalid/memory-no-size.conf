node { name db-1; }
runtime { threads auto; }
admin { token_file /etc/spacestorage/admin.token; }
disable internode;
storage { data_dir /var/lib/spacestorage; }
memory { labels { tier ram; } }
entrypoint admin { address 127.0.0.1; port 7700; handler admin; }
