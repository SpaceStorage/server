# expect: entrypoint[0].tls.certificate cert_inline_forbidden
admin { token_file /etc/spacestorage/admin.token; }
entrypoint { port 7701; handler admin-http; tls { certificate "-----BEGIN CERTIFICATE-----"; key /etc/x.key; } }
disable admin;
