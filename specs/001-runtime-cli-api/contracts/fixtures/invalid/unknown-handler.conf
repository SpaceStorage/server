# expect: entrypoint[0].handler entrypoint_unknown_handler
admin { token_file /etc/spacestorage/admin.token; }
entrypoint { port 7701; handler admin-http; }
entrypoint { port 5000; handler telnet; }
disable admin;
