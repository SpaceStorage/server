# Smallest valid configuration: one admin handler enabled, the other explicitly disabled.
admin {
  token_file /etc/spacestorage/admin.token;
}

entrypoint {
  port 7701;
  handler admin-http;
}

disable admin;
