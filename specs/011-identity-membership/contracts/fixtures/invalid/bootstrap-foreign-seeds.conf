# bootstrap with a foreign seed is refused (FR-005).
cluster {
    bootstrap;
    token_file /etc/spacestorage/join.secret;
    seeds {
        name other;
        address 10.0.0.9;
        port 7702;
    }
}
node {
    name db-1;
}
