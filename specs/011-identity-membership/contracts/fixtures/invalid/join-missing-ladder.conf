# first join must present every ladder key (`004`). This node omits `az` when the cluster ladder is `[az]`.
cluster {
    join;
    token_file /etc/spacestorage/join.secret;
    seeds {
        name db-1;
        address 127.0.0.1;
        port 7702;
    }
}
node {
    name db-2;
    labels { }
}
