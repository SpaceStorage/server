# Subsequent node: secret + ladder; waits as pending until `spacestorage admit`.
cluster {
    join;
    token_file /etc/spacestorage/join.secret;
    seeds {
        name db-1;
        address 127.0.0.1;
        port 7702;
    }
}
node {
    name db-2;
    labels {
        az az1;
    }
}
