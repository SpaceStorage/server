# Config fixtures for `011-identity-membership`

Valid snippets are appended to `016` one-node / three-node starters. Invalid files are `spacestorage validate` cases (exit 2).

`cluster.token_file` is the join secret (`004`). `bootstrap` / `join` are exclusive.
