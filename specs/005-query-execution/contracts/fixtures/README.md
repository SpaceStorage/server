# Fixtures: query execution

**Feature**: `005-query-execution`

Starter `query { }` block for a node that already has `001`/`016` entrypoints. Invalid files are `spacestorage validate` cases.

- [query-block.conf](query-block.conf) — documented defaults made explicit.
- [invalid/query-max-concurrent-zero.conf](invalid/query-max-concurrent-zero.conf)
- [invalid/query-spill-unknown.conf](invalid/query-spill-unknown.conf)
