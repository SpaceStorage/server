# Snippet — include inside a node conf that already declares entrypoints.
# Full first-binary node: specs/016-mvp-and-nongoals/contracts/fixtures/first-binary-one-node.conf

query {
    max_concurrent_per_node 512;
    max_concurrent_per_namespace 128;
    max_memory 256MiB;
    spill off;
    default_concurrency 1;
}
