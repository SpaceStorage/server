# expect: types_unknown_codec{c=brotli, known=[none, snappy, zstd, lz4]}
node { name db-1; }
admin { token_file /etc/spacestorage/admin.token; }
disable admin-http;
entrypoint admin { port 7700; handler admin; }
storage { data_dir /var/lib/spacestorage; }
types { default_compression brotli; }
