# expect: storage_data_dir_required
# A node that can hold persistent containers must declare storage { data_dir }.
node { name db-1; }
admin { token_file /etc/spacestorage/admin.token; }
disable admin-http;
entrypoint admin { port 7700; handler admin; }
types { default_compression zstd; }
